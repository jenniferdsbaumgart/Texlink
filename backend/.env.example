# Database (Docker PostgreSQL)
DATABASE_URL="postgresql://texlink:texlink123@localhost:5432/texlink?schema=public"

# JWT
JWT_SECRET="texlink-dev-secret-change-in-production"
JWT_EXPIRATION="7d"

# App
PORT=3000
NODE_ENV=development

# Redis (for rate limiting and caching)
# Optional: if not provided, in-memory fallback will be used
REDIS_URL=redis://localhost:6379

# ===========================================
# INTEGRATIONS - External Services
# ===========================================

# CNPJ Validation (ReceitaWS - fallback provider)
RECEITAWS_URL=https://receitaws.com.br/v1/cnpj
RECEITAWS_API_KEY=

# Email (SendGrid)
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@texlink.com.br
SENDGRID_FROM_NAME=Texlink

# WhatsApp (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
TWILIO_STATUS_CALLBACK=

# Credit Analysis (Serasa - future integration)
SERASA_API_URL=
SERASA_API_KEY=
SERASA_API_SECRET=
